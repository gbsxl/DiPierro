spring:
  application:
    name: Pierro

  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://postgres:5432/app
    username: postgres
    password: postgres
  flyway:
    enabled: true
    clean-disabled: false
  jpa:
    show-sql: on
    hibernate:
      ddl-auto: none